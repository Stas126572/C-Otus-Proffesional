#pragma once

#cmakedefine01 _PROJECT_VERSION_PATCH @PROJECT_VERSION_PATCH@
#define STR(str) #str
#define STRING(str) STR(str)

std::string s = STRING(_PROJECT_VERSION_PATCH);
int PROJECT_VERSION_PATCH = s[0] - '0';
